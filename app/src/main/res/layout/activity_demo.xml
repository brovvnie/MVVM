<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="com.brovvnie.mvvm.ui.main.DemoViewModel" />

        <variable
            name="vm"
            type="DemoViewModel" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:binding="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#202020"
        android:orientation="vertical">

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="网络访问"
            binding:onClickCommand="@{vm.netWorkClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="RecycleView多布局"
            binding:onClickCommand="@{vm.rvMultiClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="底部Tab按钮"
            binding:onClickCommand="@{vm.startTabBarClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ViewPager绑定"
            binding:onClickCommand="@{vm.viewPagerBindingClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ViewPager+Fragment"
            binding:onClickCommand="@{vm.viewPagerGroupBindingClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="表单提交"
            binding:onClickCommand="@{vm.formSbmClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="表单编辑"
            binding:onClickCommand="@{vm.formModifyClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="权限申请"
            binding:onClickCommand="@{vm.permissionsClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="全局异常捕获"
            binding:onClickCommand="@{vm.exceptionClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="文件下载"
            binding:onClickCommand="@{vm.fileDownLoadClick}" />
    </LinearLayout>
</layout>